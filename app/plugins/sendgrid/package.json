{
  "name": "@bermooda/plugin-sendgrid",
  "version": "0.1.0",
  "description": "Send transactional email through SendGrid. Only one email provider plugin can be active at a time.",
  "private": true,
  "bermooda": {
    "title": "SendGrid",
    "slug": "sendgrid",
    "engine": ">=0.1.0",
    "settings": [
      {
        "key": "apiKey",
        "label": "API Key",
        "type": "password"
      }
    ]
  }
}
